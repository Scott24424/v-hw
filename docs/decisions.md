# 결정 기록

- [2026-07-29] .claude/settings.json의 deny 규칙을 `Read(./.env)` → `Read(.env)` (및 `.env.*` 동일)로 수정. 공식 문서(code.claude.com/docs/en/permissions) 기준 지원되는 앵커 형식은 bare filename / `/path` / `//path` / `~/path` 뿐이며 `./path`는 명시되어 있지 않아 매칭이 보장되지 않음. bare filename 형식은 `**/.env`와 동일하게 어떤 깊이에서도 매칭됨이 문서로 확인됨.
- [2026-07-29] `Assignment.date`를 `DateTime`이 아니라 `String`("YYYY-MM-DD")으로 저장. SQLite에 DATE 타입이 없어 Prisma `DateTime`은 타임스탬프로 저장되고, KST(+9)에서 자정 경계마다 하루 밀리는 버그가 구조적으로 발생함. ISO 문자열은 사전순=시간순이라 범위 쿼리·인덱스가 그대로 동작. 대가로 날짜 유틸을 직접 만들고 단위 테스트로 덮는다.
- [2026-07-29] 시각은 `Int`(자정 기준 분)로 저장. 실물 시간표가 12시간제 표기라 "2:00"의 오전/오후가 모호한데 정수 정규화로 저장 시점에 해소되고, 정렬·겹침 검사가 산술 비교로 끝남.
- [2026-07-29] 과제 유형은 단일 `Assignment` 테이블 + `type` enum으로 모델링(유형별 분리 테이블 기각). 핵심 기능이 "유형 무관 남은 것 전부를 날짜순"이라 단일 인덱스 스캔으로 끝나야 하는데, 분리 테이블은 Prisma에 다형 관계가 없어 매번 N회 쿼리 후 앱 메모리에서 병합·재정렬해야 함. 대가인 nullable 컬럼 4개는 앱 레벨 검증으로 커버. 읽기 전용 필드가 6개를 넘으면 `ReadingDetail` 1:1 분리로 이전.
- [2026-07-29] "밀림(overdue)"은 상태로 저장하지 않고 `date < 오늘 && status ∈ {PLANNED, IN_PROGRESS}`로 계산. 저장 방식은 자정 배치가 필요한데 상시 구동 PC가 절전에 들어가면 DB가 거짓말을 하게 됨. 단 "오늘"의 기준은 서버(Asia/Seoul) 한 곳으로 두고 API가 `isOverdue` 파생 필드를 내려보냄 — 기기별 시계가 어긋나 아이패드와 폰이 다른 목록을 보여주는 것을 막기 위함.
- [2026-07-29] 시간표는 `RoutineBlock` 템플릿만 두고 날짜별 행을 실체화하지 않음. "시간 틀은 매일 동일, 내용만 다름"이라는 요구사항과 정확히 일치하고 생성 배치가 불필요. "오늘만 시간 변경/건너뛰기"가 필요해지면 `ScheduleInstance` 테이블 추가 + 행 없으면 템플릿 폴백으로, 기존 데이터 변경 없이 이전 가능.
- [2026-07-29] docs/architecture 브랜치를 main이 아니라 chore/agent-rules HEAD 기준으로 생성. main에는 `.gitignore`/`README.md`뿐이고 CLAUDE.md·decisions.md·worklog.md가 전부 미머지 상태의 chore/agent-rules에만 있어, main 기준으로 파면 두 문서를 새로 만들게 되어 충돌이 확정됨. PR base도 chore/agent-rules로 둔다.
- [2026-07-29] architecture.md §7.1 의존성 4건(vitest, zod, Tailwind CSS, create-next-app 스캐폴딩) 전부 사용자 승인. 테스트 러너는 vitest, E2E는 Playwright 추가 확정 — 사용자가 명시적으로 지정.
- [2026-07-29] 백업은 1차 버전에서 SQLite 파일 직접 사용(§0.3의 "집 PC 단독 소유" 전제 유지) + `npm run backup` 스크립트로 `prisma/dev.db`를 `backups/`에 타임스탬프 붙여 복사. 원격 복제나 자동 스케줄링은 하지 않음 — 사용자가 1차 범위를 수동 스크립트로 한정. `backups/`는 `.gitignore`에 추가(DB 스냅샷은 커밋 대상이 아님).
- [2026-07-29] 취침 시각·주말 구분을 architecture.md §7.2대로 미해결로 남기지 않고 "실물대로 간다"로 확정: 시간표는 17:00 종료, 요일 구분 없음(주말도 평일과 동일 템플릿 적용). 근거: 실물(daily-schedule.jpg)에 취침 블록과 요일 구분이 애초에 없고, 사용자가 실물을 있는 그대로 옮기는 것을 이번 범위로 확정. 취침 블록이나 주말 전용 템플릿이 필요해지면 RoutineBlock에 dayOfWeek? 필드를 추가하는 방식으로 이전 가능(기존 데이터 변경 없음).
- [2026-07-29] `prisma/schema.prisma`의 `datasource url`을 `env("DATABASE_URL")` 대신 `"file:./dev.db"` 리터럴로 고정. CLAUDE.md가 .env 파일 읽기·수정·더미값 삽입을 금지하고 있어 env() 참조 시 필요한 `.env`를 만들 수 없음(prisma generate/validate가 env var 부재 시 에러). 이 값은 비밀이 아니라 "집 PC가 SQLite 파일 하나를 단독 소유"라는 이미 확정된 배포 결정 그 자체이고, 배포 환경이 하나뿐이라 환경별로 달라질 이유가 없어 리터럴로 고정해도 손실이 없음. 여러 환경을 지원해야 하는 시점이 오면 그때 env() 참조로 전환.
- [2026-07-29] Prisma 스키마는 이번 스캐폴딩 단계에서 모델 없이 datasource/generator만 정의(빈 상태). 모델은 architecture.md §1을 근거로 후속 커밋에서 기능 단위로 추가 — 스캐폴딩 커밋과 스키마 커밋을 섞지 않는다(CLAUDE.md "기능 단위로 작게 커밋").
- [2026-07-29] vitest 기본 environment를 `jsdom`이 아닌 `node`로 설정. 이번 스캐폴딩에는 React 컴포넌트 단위 테스트 대상이 없고(날짜/진도 계산 같은 순수 로직 테스트가 우선), jsdom + 관련 패키지는 실제로 컴포넌트를 테스트하기 시작하는 시점에 추가.
- [2026-07-29] `vitest.config.ts`에 `resolve.alias`로 `@` → 프로젝트 루트를 수동 등록. `tsconfig.json`의 `paths`는 tsc/Next 빌드에만 적용되고 vitest(Vite)는 이를 자동으로 읽지 않아, alias 없이 `@/lib/...` import가 테스트에서만 실패함(`vitest.config.ts` 신설 이후 재발 가능한 함정이라 기록).
- [2026-07-29] 과제 생성 검증은 zod `superRefine`으로 architecture.md §2.2의 유형별 필드 규칙(READING만 bookId/progressUnit/progressEnd 필수, 그 외는 전부 null)을 강제. `progressStart` 자동 채움(직전 과제의 progressEnd+1)은 스키마가 아니라 라우트 핸들러에서 처리 — DB 조회가 필요해 순수 검증 로직인 zod 스키마의 책임 범위를 벗어남.
- [2026-07-29] `/api/assignments` 라우트 핸들러 자체(DB 연동)에 대한 단위 테스트는 만들지 않고, 이미 구성된 Playwright(`e2e/`)로 실제 dev 서버 + SQLite에 대고 검증. 이유: Next App Router route handler를 순수 함수로 목(mock) 테스트하려면 Request/Response 목킹 인프라를 새로 만들어야 하는데, Playwright는 이미 webServer 기동을 자동 처리하고 있어 실제 환경에 더 가깝게 검증 가능. zod 스키마·날짜 유틸 같은 순수 로직만 vitest 단위 테스트로 커버.
- [2026-07-29] `e2e/assignments-api.spec.ts`에 `test.describe.configure({ mode: "serial" })` 추가. Playwright의 `beforeAll`은 파일을 실행하는 워커마다 한 번씩 실행되는데, 기본 병렬 워커 설정에서 같은 제목의 Book을 동시에 생성하려다 unique 제약 충돌 발생(실제로 재현·확인함). DB 상태를 공유하는 e2e 스펙 파일은 앞으로도 serial 모드를 기본으로 고려할 것.
- [2026-07-29] `POST/PATCH /api/books`에서 `(title, language)` unique 제약 위반(Prisma P2002)은 400이 아니라 **409 Conflict**로 응답. architecture.md §5.2 공통 규약("검증 실패는 400, 없는 리소스는 404, 허용되지 않은 상태 전이는 409")이 이 케이스를 명시하진 않지만, "입력 자체는 유효한데 서버 상태와 충돌"이라는 409의 정의에 더 맞음(400은 요청 형식·값 자체가 잘못된 경우). `PATCH /api/books/:id`에서 존재하지 않는 id(Prisma P2025)는 §5.2 그대로 404.
- [2026-07-29] `updateBookSchema`는 빈 객체(`{}`)를 `refine`으로 거부. PATCH인데 아무 필드도 없는 요청은 클라이언트 실수일 가능성이 높고, 허용해봤자 no-op이라 명시적으로 막아 오류를 빨리 드러냄.
- [2026-07-29] `e2e/assignments-api.spec.ts`의 테스트용 책 시드를 `prisma.book.create` 직접 호출에서 `POST /api/books` 호출로 교체(이번 기능으로 엔드포인트가 생겼으므로 앞서 worklog에 남긴 "남은 이슈" 해소). 정리(afterAll)는 여전히 Prisma 직접 삭제 — `/api/books`에 DELETE가 없고, 테스트 하네스가 시스템 자체를 대표하지 않으므로 무방.
- [2026-07-29] `DELETE /api/routine-blocks/:id`는 성공 시 본문 없이 204를 반환. §5.1이 물리 삭제로 이미 확정했고, 삭제 결과로 돌려줄 리소스 표현이 없는 표준적인 경우라 200+빈객체보다 204가 정확함.
- [2026-07-29] `RoutineBlock` 삭제 시 연결된 `Assignment.routineBlockId`를 애플리케이션 코드에서 별도로 정리하지 않음. 초기 마이그레이션(`20260729053325_init`)의 FK 제약이 이미 `ON DELETE SET NULL`로 생성되어 있어(옵셔널 관계의 Prisma 기본 동작) DB 레벨에서 자동으로 처리됨 — 라우트 핸들러가 신경 쓸 필요 없음.
- [2026-07-29] `GET /api/routine-blocks`는 `/api/assignments`와 달리 쿼리 파라미터(예: `isActive`)를 받지 않음. architecture.md §5가 이 엔드포인트에 필터를 명시하지 않았고, 시간표 관리 화면(`/manage/routine`)은 활성·비활성 블록을 모두 봐야 편집이 가능하므로 전체 목록을 `sortOrder` → `startMinute` 순으로 반환. 날짜별 활성 블록만 필요한 소비처(`/api/days/:date`)는 그 엔드포인트 자체에서 `isActive` 필터를 걸 것.
- [2026-07-29] `updateRoutineBlockSchema`의 `startMinute < endMinute` 검증은 두 필드가 **같은 요청에 함께** 있을 때만 적용. 한쪽만 부분 수정하는 요청에서 기존 저장값과 비교하려면 DB 조회가 필요해 순수 zod 검증의 책임 범위를 벗어남(§2.2의 `progressStart` 자동 채움과 동일한 이유로 스키마 밖에 둠). 다만 이 케이스는 라우트 핸들러에서 DB 조회로 보강하지 않았음 — 편도 부분 수정으로 역전된 구간이 일시적으로 저장될 수 있는 게 남은 이슈.
- [2026-07-29] `PATCH /api/assignments/:id`는 `type`과 `status`를 수정 대상에서 제외. `type`은 사실상 불변(바꾸면 §2.2의 유형별 필드 불변식이 통째로 재검증돼야 함)이고, `status`는 architecture.md §5.1이 이미 `/status` 전용 엔드포인트로 분리하기로 결정했으므로 일반 PATCH에 다시 넣으면 그 분리 의도가 무너짐. 이번 작업 범위(사용자가 명시적으로 PATCH/DELETE/status만 지정)에서 `GET /api/assignments/:id`(단건 조회, architecture.md §5에는 있음)는 만들지 않음 — worklog에 남은 이슈로 기록.
- [2026-07-29] `updateAssignmentSchema`는 READING 여부에 따른 필드 허용 규칙(§2.2)을 스키마 자체에서 검증하지 않는다. 수정 시점엔 기존 레코드의 `type`을 알아야 판단 가능한데 이는 DB 조회가 필요해 zod의 책임 범위 밖(§ "progressStart 자동 채움"과 동일 논리) — 대신 라우트 핸들러가 `findUnique`로 기존 레코드를 가져와 검증한다. `bookId`는 PATCH로 null 삭제를 허용하지 않음(READING 불변식이 깨짐) 반면 `note`/`routineBlockId`는 nullable로 열어 명시적 삭제·연결 해제를 허용 — books의 `updateBookSchema` 관행과 동일.
- [2026-07-29] `PATCH /api/assignments/:id/status`의 상태 전이 규칙은 architecture.md §3.1 표를 그대로 코드화(`lib/assignments/status.ts`)하며, 표에 없는 동일 상태로의 요청(예: PLANNED→PLANNED)도 "허용 안 됨"으로 취급해 409를 반환한다. 표가 self-loop을 명시하지 않았고 굳이 no-op을 허용할 이유도 없어 문자 그대로 해석.
- [2026-07-29] `e2e/assignments-api.spec.ts`의 `afterAll`이 쓰던 `title: { contains: "e2e" }` 넓은 삭제 필터를 이번에 추가한 `e2e/assignments-detail-api.spec.ts`가 트리거함 — `fullyParallel: true` 환경에서 서로 다른 파일이 동시에 실행되는데, 이 파일의 정리 로직이 제목에 "e2e"가 들어간 다른 파일의 아직 사용 중인 assignment까지 지워 간헐적 404를 유발했다(실제 재현·확인함). `books-api.spec.ts`/`routine-blocks-api.spec.ts`가 이미 쓰던 "생성한 id만 정확히 추적해 지우기" 패턴으로 교체해 해결. 앞으로 e2e assignment 정리 로직은 title 문자열 매칭 대신 id 추적을 기본으로 한다.
- [2026-07-30] "이번 주"의 날짜 경계는 달력 주(월요일~일요일, KST) 기준으로 확정 — 롤링 7일이 아님. architecture.md에 명시돼 있지 않아 사용자에게 직접 확인 후 결정(CLAUDE.md "설계 문서와 모순되는/불명확한 구현" 질문 기준 적용). `lib/date.ts`의 `weekRangeKST()`로 구현.
- [2026-07-30] `/api/summary/remaining`의 세 버킷(overdue/today/thisWeek)은 서로 배타적인 날짜 구간(`date<오늘` / `date=오늘` / `오늘<date<=이번 주 일요일`)이며 상태 조건은 셋 다 동일하게 `isOverdue`와 같은 REMAINING 상태 집합(`PLANNED`, `IN_PROGRESS`)만 포함한다 — DONE/SKIPPED는 어느 버킷에도 나타나지 않는다. architecture.md §5.1 "남은 숙제가 핵심"이라는 설계 의도를 그대로 따름. `counts`는 각 배열의 길이를 그대로 담은 `{ overdue, today, thisWeek }` — 별도 정의가 없어 가장 직접적인 해석을 채택.
- [2026-07-30] `e2e/summary-remaining-api.spec.ts`의 "이번 주 경계" 테스트는 오늘이 이번 주 일요일인 날에는 `test.skip`으로 건너뛴다. Playwright e2e는 실제 구동 중인 dev 서버에 대고 실행돼 vitest의 `vi.setSystemTime`처럼 서버 시간을 목킹할 수 없고, 오늘이 일요일이면 "내일이면서 이번 주인 날짜"가 논리적으로 존재하지 않아(그 다음 날은 이미 다음 주) 결정적으로 재현 불가능하다. 대신 "이번 주 일요일 다음날(항상 다음 주)"이 세 버킷 어디에도 없는지는 요일과 무관하게 항상 실행되는 별도 케이스로 커버한다.
- [2026-07-30] `GET /api/days/:date`의 응답 형태는 architecture.md §5 표에 JSON 스키마가 명시돼 있지 않아(다른 엔드포인트와 달리) §4.1/§5.1/§6.3을 근거로 직접 설계: `{ date, blocks: [{ ...RoutineBlock, assignments: Assignment[] }], unlinkedAssignments: Assignment[] }`. `blocks`는 §4.1대로 `isActive` 블록만, `assignments`는 각 블록에 중첩(§6.3 "블록마다 그날 연결된 과제"). §5 표 문구의 "미연결 과제"를 최상위 `unlinkedAssignments`로 반영.
- [2026-07-30] `routineBlockId`가 있지만 그 블록이 (비활성화 등으로) 응답의 `blocks`에 없는 과제도 "미연결"로 취급해 `unlinkedAssignments`에 포함시킨다(엄밀히는 null이 아니지만). 그렇지 않으면 블록을 비활성화하는 즉시 그 블록에 연결된 과제가 오늘 화면 어디에도 안 보이게 되는데, 이는 이 앱이 명시적으로 피하려는 "밀린 것이 영원히 묻힌다"(worklog 참고) 실패 양상과 동일한 결함이라 판단해 방어적으로 처리. `lib/days/view.ts`의 `buildDayView`로 순수 함수화해 이 케이스를 단위 테스트로 커버.
