# CLAUDE.md

## 프로젝트 개요
- v-hw: 남은 방학숙제 관리 웹앱
- 스택: Next.js(App Router) + TypeScript + Prisma + SQLite
- 설계 문서: docs/architecture.md — 구현 전 항상 먼저 확인한다

## 완료 조건 (Definition of Done)
아래를 모두 통과해야 작업 완료로 간주한다. 하나라도 실패하면 완료가 아니다.
- `npm run lint` 무경고
- `npx tsc --noEmit` 타입 에러 0
- `npm run test` 전체 통과
- `npm run build` 성공

## 자가 수정 루프
- 검증 실패 시 즉시 코드를 바꾸지 말고, 원인 가설을 3개 세우고 확인 방법을 정한 뒤 검증한다.
- 가설 검증 → 수정 → 재실행을 최대 3회까지 스스로 반복한다.
- 3회 후에도 실패하면 중단하고, 시도한 가설과 각각의 실제 출력을 정리해 보고한다.
- 이전 가설이 왜 틀렸는지 설명한 뒤 다음 가설로 넘어간다.

## 판단 기준 — 질문할 때와 진행할 때
아래에 해당하면 **반드시 멈추고 질문한다**:
- 비밀 값·인증 정보가 필요한 경우
- 설계 문서와 모순되는 구현이 필요한 경우
- 데이터 삭제나 스키마 파괴적 변경이 필요한 경우
- 새 외부 의존성(패키지·API)을 추가해야 하는 경우

그 외 사소한 선택(변수명, 파일 배치, 내부 함수 분리 등)은 질문하지 않고
합리적으로 결정한 뒤 docs/decisions.md에 한 줄로 기록한다.

## 작업 기록 (비동기 검토용)
- 작업 단위마다 docs/worklog.md에 append 한다.
- 형식: [날짜/시각] 작업명 / 변경 파일 / 검증 결과(실제 출력 요약) / 남은 이슈
- 사용자가 자리에 없을 때 진행 상황을 파악하는 유일한 수단이므로 빠뜨리지 않는다.

## 개발 규칙
- 커밋은 Conventional Commits (feat:, fix:, test:, docs:, chore:)
- 기능 단위로 작게 커밋한다. 한 커밋에 여러 기능을 섞지 않는다.
- 새 기능은 단위 테스트와 함께 작성한다. 정상 케이스와 규칙 위반 케이스를 모두 포함한다.
- any 타입 금지.
- 작업 브랜치에는 자유롭게 push 한다. 브랜치명: feature/*, fix/*, chore/*, docs/*
- PR을 생성하면 그 자리에서 바로 판단한다: 아래 "자동 머지 범위"에 해당하면 같은 turn 안에서
  `gh pr merge --auto --squash --delete-branch`까지 실행한다. "생성만 하고 나중에 판단"은 하지 않는다.
- 범위를 벗어나면 PR만 생성하고 사용자 승인을 기다린다. 즉시 `gh pr merge`(auto 플래그 없이)는 절대 실행하지 않는다.
- 완료 보고에는 반드시 다음 중 하나를 명시한다: "auto-merge 예약함(gh pr merge --auto 실행 완료)"
  또는 "자동 머지 범위 아님 — 사유: <금지 경로/사유>. 사용자 승인 대기."
- 완료 보고 시 실행한 명령과 실제 출력을 함께 제시한다. 출력 없는 완료 보고는 금지.

## 금지 사항
- .env 파일 읽기·수정·더미값 삽입 금지. 값이 필요하면 질문한다.
- 마이그레이션 파일 직접 수정 금지. prisma migrate 사용.
- main 브랜치 직접 커밋·push 금지.
- 테스트를 통과시키기 위해 테스트 자체를 약화시키거나 삭제하는 것 금지.
- 확신 없이 추측으로 진행 금지.

## 자동 머지 범위
아래 조건을 모두 만족할 때만 auto-merge를 예약한다. 하나라도 걸리면 사람 승인 대기.

**허용**: UI/스타일, 문서, 테스트 추가·수정, 동작 변경 없는 리팩터링, 의존성 패치 버전,
단위·e2e 테스트를 포함한 신규 API 라우트(아래 금지 경로에 해당하지 않는 경우).

**금지 (변경 파일에 아래 경로가 하나라도 포함되면 자동 머지 금지)**:
- `prisma/migrations/**`
- 인증·권한 관련 파일 (`**/auth/**`, `**/*permission*`)
- 결제·정산 관련 파일 (`**/*payment*`, `**/*billing*`)
- `.env*`, `**/secrets/**`
- `package.json`의 의존성 메이저 버전 변경

애매하면 금지 쪽으로 판단하고, PR 본문에 왜 자동 머지 대상에서 제외했는지 이유를 적는다.

## 코드 리뷰 대응
- Codex 리뷰 지적은 먼저 코드로 타당성을 확인한 뒤 수정한다.
- 타당하지 않다고 판단되면 근거를 제시하고 수정하지 않는다.